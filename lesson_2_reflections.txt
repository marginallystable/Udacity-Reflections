What happens when you intialize a repository? Why do you need to do it?

An empty repository is created.  This sets stuff up for your first commit.

How is the staging area different from the working directory and the repository?  What value do you think it offsets?

The staging area is an intermediate transition zone that allows the user to isolate and group changes from the working directory prior to committing them.

How can you use the staging area to make sure you have one commit per logical change?

The staging area can isolate logical changes from all changes in the working directory.

What are some situations when branches would be helpful in keeping your history organized?  How would branches help?

Bug fixes to deployed versions.

How do the diagrams help you visualize the branch structure?

They show all of the commits and their parents.

What is the result of merging two branches together?  Why do we represnt it in the diagram the way we do?

Two branches become one integrated branch with changes from both.

What are the pros and cons of Git's automatic merging vs. always going merges manually?

Auto merging is very efficient for unconflicted changes.  However, it is easy to confuse.
